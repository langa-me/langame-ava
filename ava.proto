syntax = "proto3";

package ava;

service Ava {
   rpc Call(stream AvaRequest) returns (stream AvaResponse) {}
}

message AvaRequest {
    string message = 1;
}
message AvaResponse {
    string message = 1;
}